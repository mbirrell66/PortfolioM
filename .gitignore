# Python
__pycache__/
*.pyc
.pytest_cache/

# Build / packaging output
build/
dist/
.venv-build/
*.exe
installer/Output/

# Local database (contains personal data)
*.db

# Installer build output is huge (see installer/Output/ above), but the .iss
# script itself is the release recipe and must stay tracked.

# Stale build output kept for reference
build.old/
build1.old/